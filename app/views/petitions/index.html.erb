<h2><%= params[:my] ? 'Мои' : 'Все' %> петиции</h2>

<table class="table">
  <thead>
  <tr>
    <th>Петиция</th>
    <th>Автор</th>
  </tr>
  </thead>
  <% @petitions.each do |petition| %>
    <%= render 'petition', petition: petition %>
  <% end %>
</table>
